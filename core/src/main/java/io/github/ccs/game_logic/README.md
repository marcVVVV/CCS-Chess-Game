# Game Logic Team

Owns chess rules, turn flow, move validation, board state, win/draw conditions, and gameplay systems.

Keep rendering concerns in `ui` and external communication concerns in `backend` or `ai_networking`.

# UI Team

Owns screens, layouts, HUDs, menus, input presentation, and visual state.

Start here when adding LibGDX `Screen` implementations, scene composition, buttons, overlays, or display-only formatting.
